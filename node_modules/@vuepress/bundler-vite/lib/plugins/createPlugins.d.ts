import type { App } from '@vuepress/core';
import type { Plugin } from 'vite';
import type { ViteBundlerOptions } from '../types';
export declare const createPlugins: ({ app, options, isBuild, isServer, }: {
    app: App;
    options: ViteBundlerOptions;
    isBuild: boolean;
    isServer: boolean;
}) => Plugin[];
